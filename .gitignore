# 注意：.gitignore 不支援行末註解。
# 寫成「data/  # 說明」時，整行連空白和 # 都算樣式的一部分，
# 結果是什麼都匹配不到 —— 曾經因此差點把聲紋與逐字稿 commit 進去。
# 註解一定要獨立一行。

.venv/
__pycache__/
*.pyc

# 會議資料庫：逐字稿與聲紋都在裡面，絕對不能進版控。
# 音訊暫存（錄音模式）也在 data/spool/ 底下。
data/

# 下載的模型權重（whisper 數 GB、ECAPA 約 80MB）。
# 用 requirements.txt 重建即可，不該進版控。
models/

.DS_Store

# 給 Claude Code 看的專案筆記。留在本機，不公開 ——
# 裡面包含這台機器的環境細節（權限狀況等），對外沒有價值也不該外流。
CLAUDE.md
